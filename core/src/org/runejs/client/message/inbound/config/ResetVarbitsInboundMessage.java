package org.runejs.client.message.inbound.config;

import org.runejs.client.message.InboundMessage;

/**
 * A message telling the client to reset all its varbits.
 *
 * This message has no properties.
 */
public class ResetVarbitsInboundMessage implements InboundMessage {
}
